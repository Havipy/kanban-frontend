import React from 'react';
import cl from './BoardsMenuFooter.module.scss';

const BoardsMenuFooter: React.FC = () => {
	return (
		<footer className={cl.footer}></footer>
	)
}

export default BoardsMenuFooter;